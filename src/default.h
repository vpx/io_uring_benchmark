#ifndef DEFAULT_H
#define DEFAULT_H

enum defaults {
    default_sq_entries = 1024,
    default_cq_entries = 2048,
    default_sq_idle = 1000,
    default_sq_poll = 0,
    default_buff_size = 1024,
    default_buffs_entries = 4096
};

#endif // DEFAULT_H
